# Excerpt: qmd (local markdown search) — illustrative offline extract for demo

qmd is a local search engine for markdown files. It combines BM25 and vector
search with optional LLM re-ranking, running on-device.

CLI and MCP interfaces let an agent shell out or call tools natively.
It indexes a directory of notes; it does not compile a wiki by itself.

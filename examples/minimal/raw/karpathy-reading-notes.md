# Reading notes: Karpathy's llm-wiki idea

**These are our words, not his.** This file is a restatement written inside this
project, which makes it `own-corpus`: it can be linked from wiki prose at L1, and
it can never back an L2 pin. Restating an external source does not make the
restatement external evidence.

Our reading: compiled knowledge should sit between the human and the raw
sources; an ingest updates many wiki pages rather than one; the wiki compounds
where a chat transcript does not.

To pin any of that at L2, capture bytes from the gist itself and register them
as `external-primary`.
